# Settings

Reached from the bottom of the sidebar on desktop, or **More > Settings** on
mobile.

## Appearance

**Theme** switches between **Dark** (the default) and **Light**. The choice is
saved and applied before the first paint, so there is no flash of the wrong
theme on the next visit.

**Category switching** controls how the sidebar responds:

- **Hover** (default) switches category as the pointer moves over it.
- **Click** requires a click.

Hover is quicker for browsing; click is better if you find the sidebar changing
under you. The setting only affects the desktop sidebar.

## Favorites & custom tables

Shows how many tables you have starred and how many custom tables you have
written.

- **New custom table** opens the editor. See
  [Favorites and Custom Tables](Favorites-and-Custom-Tables).
- **Clear all favorites** unstars everything. Custom tables are untouched.
- **Clear all custom tables** deletes every table you have written. It asks
  twice, because nothing restores them. Export first if you are unsure.

Both buttons are greyed out when there is nothing to clear.

## Backup & transfer

**Export** and **Import** for favorites, custom tables and roll history. See
[Backup and Transfer](Backup-and-Transfer).

## Sources & attribution

Credits for the table sources and the libraries used, along with links to the
credits and changelog pages and the licence.
